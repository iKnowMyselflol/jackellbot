const { RichEmbed } = require('discord.js');
const ms = require('ms');
const fs = require('fs');
module.exports = {
  name: "warn",
  category: "moderation",
  description: "Gives a user a warning!",
  run: async (client, message, args) => {
  if(!message.member.hasPermission("MANAGE_MEMBERS")) return message.channel.send("You cannot use this command. Please contact a staff m")
  }
}
   